tosca_definitions_version: tosca_simple_yaml_1_0

description: Openstack Neutron Network

node_types:
  tosca.nodes.Custom:
    derived_from: tosca.nodes.Root

topology_template:
  node_templates:
    node:
      type: tosca.nodes.Custom
